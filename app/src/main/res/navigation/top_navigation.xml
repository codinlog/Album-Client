<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/tap"
    app:startDestination="@id/photoFragment">

    <fragment
        android:id="@+id/albumFragment"
        android:name="com.codinlog.album.Controller.Fragment.AlbumFragment"
        android:label="album"
        tools:layout="@layout/album_fragment" />
    <fragment
        android:id="@+id/timeFragment"
        android:name="com.codinlog.album.Controller.Fragment.TimeFragment"
        android:label="time"
        tools:layout="@layout/time_fragment" />
    <fragment
        android:id="@+id/photoFragment"
        android:name="com.codinlog.album.Controller.Fragment.PhotoFragment"
        android:label="photo"
        tools:layout="@layout/photo_fragment" />
</navigation>